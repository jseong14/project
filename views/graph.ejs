<!DOCTYPE html>
<html>
<head>
  
  <!-- Required meta tags -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!--Char.js 불러오기-->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.7.0/chart.js"></script>
  <title>Autofarm</title>
</head>
<body>
  <!--navbar 설정-->
  <nav class="navbar navbar-expand navbar-light" style="background-color: #e3f2fd;">
    <a class="navbar-brand" href="home" style="font-size: 1.5em;">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="data">Data <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="graph">Graph</a>
        </li>
      </ul>
    </div>
  </nav>

  <div>
    <h1 class="display"><span style="font-weight:bold; font-size:1em">Graph Values</span>
      <button type="button" class="btn btn-outline-primary">
        <a href = "javascript:location.reload()" target = "_self">Refresh</a>
    </h1>
    <p class="lead"><span style="font-weight:bold"> 아래에 항목을 클릭하여 원하시는 그래프를 확인할 수 있습니다!</span></p>
  </div> 
<tbody>
  <div>
    <canvas id="myChart" width="400" height="320"></canvas>
  </div>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const chartData = <%- JSON.stringify(chartData) %>;
      const ctx = document.getElementById('myChart').getContext('2d');

      const colors = ['Brown', 'Black', 'orange', 'Red', 'Blue'];

      const data = {
        labels: chartData.date,
        datasets: [
          {
            label: '토양수분',
            data: chartData.soil,
            backgroundColor: 'transparent',
            borderColor: colors[0],
            borderWidth: 4,
          },
          {
            label: '가스',
            data: chartData.gas,
            backgroundColor: 'transparent',
            borderColor: colors[1],
            borderWidth: 4,
          },
          {
            label: '조도',
            data: chartData.light,
            backgroundColor: 'transparent',
            borderColor: colors[2],
            borderWidth: 4,
          },
          {
            label: '온도',
            data: chartData.temp,
            backgroundColor: 'transparent',
            borderColor: colors[3],
            borderWidth: 4,
          },
          {
            label: '습도',
            data: chartData.humi,
            backgroundColor: 'transparent',
            borderColor: colors[4],
            borderWidth: 4,
          }
        ]
      };

      const options = {
        responsive: true,
        maintainAspectRatio: false,
      };

      new Chart(ctx, {
        type: 'line',
        data: data,
        options: options
      });
    });
  </script>
  </tbody>
</body>
</html>
